import { BusinessToCustomerScheduling } from './business-to-customer-scheduling.model';

describe('BusinessToCustomerScheduling', () => {
  it('should create an instance', () => {
    expect(new BusinessToCustomerScheduling()).toBeTruthy();
  });
});
