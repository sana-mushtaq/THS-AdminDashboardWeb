import { AppointmentStatusLog } from './appointment-status-log.model';

describe('AppointmentStatusLog', () => {
  it('should create an instance', () => {
    expect(new AppointmentStatusLog()).toBeTruthy();
  });
});
