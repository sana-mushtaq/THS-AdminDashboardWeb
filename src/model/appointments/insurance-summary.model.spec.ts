import { InsuranceSummary } from './insurance-summary.model';

describe('InsuranceSummary', () => {
  it('should create an instance', () => {
    expect(new InsuranceSummary()).toBeTruthy();
  });
});
